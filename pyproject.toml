[project]
name = "eventbus"
version = "0.1.0"
description = ""
authors = [
    {name = "Ben Liu",email = "ben.liu@homee.ai"}
]
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
    "python-dotenv>=1.1.1",
    "pydantic>=2.11.7",
    "google-cloud-pubsub==2.33.0",
    "opentelemetry-exporter-otlp-proto-http(>=1.27.0,<1.40.0)",
    "python-json-logger (>=4.0.0,<5.0.0)",
    "rich (>=14.2.0,<15.0.0)",
    "pydantic-settings (>=2.12.0,<3.0.0)",
    "google-cloud-storage (>=3.7.0)"
]

[tool.poetry.scripts]
eventbus-cli = "eventbus.cli.cli:main"

[project.scripts]
eventbus-cli = "eventbus.cli.cli:main"

[tool.ruff]
preview = true
line-length = 150
exclude = [
    "tmp*",
]